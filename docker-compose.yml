services:
  backend:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    ports: ["5000:5000"]
    environment:
      FLASK_PORT: 5000
  frontend:
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
    ports: ["3000:80"]
    depends_on: [backend]

